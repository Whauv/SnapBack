"""Placeholder tests for exporter service modules."""

import unittest


@unittest.skip("Exporter-specific tests will be added in a later pass.")
class ExporterPlaceholderTests(unittest.TestCase):
    """Keep the exporter test package discoverable until real coverage exists."""

    def test_placeholder(self) -> None:
        """Provide a lint-clean placeholder test."""
        self.assertTrue(True)
