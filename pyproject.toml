[tool.ruff]

[tool.ruff.lint]
# Enforce a stable CI baseline on the existing codebase:
# - F catches unresolved names and other correctness issues
# - I keeps imports consistent
# - UP applies safe modernization fixes
select = ["F", "I", "UP"]
