[tool.ruff]

[tool.ruff.lint]
# D203 and D211 are mutually exclusive
# D212 and D213 are mutually exclusive
# COM812 is recommended to be disabled when using the formatter
ignore = ["D203", "D213", "COM812"]
select = ["ALL"]
