# Meet Extension

This folder contains the Google Meet Chrome extension surface.

- `src/` holds executable extension scripts such as the background worker, content script, and UI controllers.
- `public/` holds static HTML and CSS assets loaded by the manifest.
- `manifest.json` wires the packaged paths together for Chrome.
