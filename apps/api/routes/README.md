# API Routes

HTTP route modules for the SnapBack FastAPI surface.

- `health.py` exposes the root redirect and health endpoint
- `session.py` handles session lifecycle, transcripts, and audio chunks
- `recap.py` handles recap generation
- `export.py` handles PDF, Markdown, and Notion exports
- `study.py` handles study pack generation
